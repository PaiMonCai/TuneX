# TuneX 开发方案：个人与团队 SaaS

> 状态：执行中。本文中的"已有"指代码入口存在，不代表在真实网络或生产环境通过验收。
> 产品决策：**TuneX 是多租户 SaaS**；个人用户和团队共享托管控制面，部署自己的节点。产品以隧道可用性、协作与资源管理为核心；支付等强商业模块降级为可选扩展，默认关闭，**不是删除项目的 SaaS 属性**。
> `DEVELOPMENT.md` 是现有实现/运维记录；`reports/` 中的结论须重测。方案执行中，每个阶段完成后回填本文件的验收状态。
>
> **2026-09-24 进度回填**：SEC-01/SEC-02（部分）/TEN-01（后端）/TEN-02（部分）/AUTHZ-01/AUTHZ-02（策略模型部分）/PAY-01/QA-01 已完成并合入 main（CI 全绿，含真实 MySQL 迁移验证）；详见 `reports/tunex-vs-relayx-diff.md` 与 §6 工作包状态列。**重要修正**：原版 RelayX 的 agent 数据面同样只有裸 TCP/UDP，mtls/quic 等协议枚举两边均未实现——差距在流量计量与验证深度，不在协议引擎。

## 1. 产品定位、场景与边界

### 1.1 用户和价值

- **个人**：注册 TuneX，自动创建个人工作空间，部署内网 Agent，把自有服务安全地发布到公网入口；查看节点状态、连接错误和用量。
- **团队**：在同一托管平台创建团队工作空间，邀请成员、划分角色、共享隧道和节点；相互独立的团队绝不能读取或修改对方资源。
- **平台管理员**：管理平台共享入口、容量、异常和审核；不等同于任意工作空间的 owner。若确需代管，必须单独授权并留审计记录。
- **权限优先、商业可选**：保留参考项目值得借鉴的细粒度角色权限、节点组准入、协议许可和流量/带宽/连接额度，但由独立的授权策略决定，与购买流程解耦。基础使用不要求充值；支付等模块只有在运营成本或明确需求需要时才启用，绝不成为正确实现权限的技术前提。

### 1.2 与参考项目的关系

RelayX 的[产品功能概览](https://docs.relayx.cc/guide/intro/)、[版本对比](https://docs.relayx.cc/guide/type/)、[套餐配置](https://docs.relayx.cc/deploy/config/plan)、[节点组配置](https://docs.relayx.cc/deploy/config/node-group)、[后台 RBAC](https://docs.relayx.cc/deploy/config/rbac)、[协议说明](https://docs.relayx.cc/guide/advanced/protocol/)与[私有部署介绍](https://docs.relayx.cc/deploy/intro/)只作为需求和使用场景参考。**TuneX 不是 RelayX 的分发版，也不以复制其协议、密钥、授权机制或商业运营面板为目标。**SaaS 架构与产品体验需独立实现。

> 法律/来源门槛：仓库内存在“原版二进制提取的固定密钥”和授权仿真实现的注释。非商业或免费并不意味着自动获得代码、资产、商标和密钥的使用权。对外发布前完成来源、依赖许可与权利审查；未确认的代码应隔离或独立重写。此处不作法律结论。

### 1.3 第一版不做什么

- 不把公开售价、充值、余额、支付通道、佣金、提现、营销优惠券作为注册/建隧道前置步骤。
- 不在未通过真实双节点流量测试前宣称 QUIC/WSS/MTLS/自研协议、多跳、自动故障转移或高性能加密能力。
- 不开放未经审查的上游 Agent 安装脚本/授权服务；不将当前 mock 页面视作真实 SaaS 能力。
- 自托管可作为后续部署形态，但**不能以单实例单组织模型替代多租户 SaaS 的设计和验收**。

## 2. 当前项目快照与可用性判断

| 模块 | 当前证据 | 距离 SaaS 可用的差距 |
| --- | --- | --- |
| 技术栈 | Web：Next.js 16/React 19；API：Bun/Hono/Prisma；Agent：Go；Compose 包含 MySQL、Redis、Worker、Caddy。 | 先沿用现有栈，先做正确性和安全性，再根据容量测量优化部署。`docker-compose.yaml` 当前更像开发栈，不能直接当公网生产配置。 |
| 页面/API | `web/src/app` 有注册、登录、仪表盘、隧道、节点、套餐、充值、工单、后台；`web/src/lib/api.ts` 可切换 mock/真实请求；`backend/src/routes` 有相应接口。 | 页面存在不证明真实模式可用；用户旅程应在 mock 关闭时逐条验收；商业页面默认隐藏但底层数据暂不硬删。 |
| 数据模型 | `backend/prisma/schema.prisma` 有 User、Tunnel、Node、NodeGroup、Plan、订单、余额与 AdminRole。 | 缺明确的 Workspace/Membership 归属；多数查询没有强制 workspace 作用域。当前后台 RBAC 不能代替租户隔离。 |
| Agent/协议 | `agent/internal/engine/runtime.go` 有 TCP/UDP 基础监听转发路径；界面和枚举列有更多协议。 | 公网入口到无入站端口的内网 Agent 的可信反向数据链路，需要单独证明；“枚举包含”不等于协议真正受支持。 |
| 多节点/在线状态 | Socket 注册/心跳/配置推送与断线标记有代码，仓库有多节点报告。 | 离线回收未闭环，端口冲突/监听失败缺控制面反馈；历史报告不可直接作为当前验收。 |
| 后台任务 | `backend/src/worker.ts` 调度约 10 项工作，多个 handler 返回计数或占位说明。 | 不可把注册 cron 等同于流量入库、重试、续费或清理；未实现任务要停调度并给出明确状态。 |
| 安全/交付 | `backend/src/crypto/keys.ts` 与 `agent/internal/license/license.go` 含上游固定密钥；`backend/src/env.ts` 有开发默认秘密；无仓库内自动化测试/CI；Web 构建与 Linux 目标 Go 编译曾通过。 | 公网多租户场景中，来源/密钥/隔离/越权/可复现 CI 全是发布阻断项。构建通过不等于安全或可运营。 |

**证据分级**：源码 = 有入口；自动化网络/权限测试 = 已验证；灰度运行与故障演练 = 可公开。仓库开发规范及报告标有 **2026-09-24** 的“实测”；是否可作为发布证据仍须按当前工作区复现并保存记录。

## 3. 功能范围与里程碑

| 功能 | 0.1 邀请制 SaaS Beta（必需） | 0.2 稳定团队版 | 后续按需求与证据再做 |
| --- | --- | --- | --- |
| 账号/空间 | 注册或邀请（可由运营控制开关）；自动创建个人 workspace；团队空间及邀请；邮箱验证、密码重置；固定 owner/admin/member/viewer 角色 | 自定义资源读写角色、细粒度共享和团队审计视图 | 企业 SSO、跨组织协作 |
| 租户隔离 | 所有租户资源（节点、隧道、API Key、流量、任务）强制 workspace 作用域；平台资源单独建模 | 跨空间切换体验、细粒度共享、配额治理 | 多地域隔离部署 |
| 隧道 | 私网 Agent 无需开放入站端口；公网入口到私网目标的 **TCP** 可用链路；新建/启停/删除、失败提示 | UDP、多入口与策略化端口分配 | QUIC、WSS、多跳等逐项独立验证 |
| 额度 | 系统/管理员直接授予独立能力策略，保留节点组/协议/限速/流量/并发等丰富约束；无需订单 | 配额模板、策略到期和精细授权审计 | 可选付费套餐仅负责发放策略，不接管权限判定 |
| 授权策略 | 免费工作空间自动获得完整基础能力策略：节点组/协议白名单、隧道数、流量、带宽、并发、IP 上限；独立于订单 | 管理员可分配/撤销能力模板、受限时有原因与审计；可定义角色 | 更复杂的策略模板与条件规则按需扩展 |
| 观测 | Agent 在线状态、失败原因、真实基础流量计数、日志与报警 | 趋势分析、告警订阅、保留期配置 | 商业级 SLA 与复杂成本分摊 |
| 商业模块 | 服务端关闭支付/充值/返佣/提现入口和回调；免费额度已经独立于订单，现存商业数据只读保留 | 收口独立可选计费适配层及旧数据归档 | 商业功能仅有明确需求、合规评估和完整测试后启用 |
| 部署 | 官方托管环境、隔离的测试环境、Linux Agent 与版本化镜像 | 生产备份/恢复演练、多副本控制面 | 自托管部署指南和安装包 |

**0.1 的 SaaS 最小可行定义**：至少两个互不关联的 workspace 在同一控制面中同时正常使用；各自绑定自己的 Agent 与隧道；任何用户、API Key、任务、配置推送、缓存和统计均不能串租户。一个个人 workspace 与一个两成员团队 workspace 的测试必须同时通过。

## 4. 架构和数据契约

```text
用户浏览器 ── HTTPS ──> Caddy ──> Next.js Web / Hono API ──> MySQL（租户持久化）
                                     │                  └────> Redis（带租户标识的短期状态/队列）
                                     └────> Agent 控制通道（节点身份绑定 workspace）
公网访问者 ─ TCP ─> 平台共享/租户专属入口 Agent ═ 认证加密数据通道 ═> 私网 Agent ─> 用户服务
```

### 4.1 多租户与授权（发布 P0）

1. 引入 `Workspace`、`WorkspaceMember`、`WorkspaceInvite`、`WorkspaceApiKey`、`AuditEvent`；个人空间和团队空间采用相同隔离模型。当前 User/`AdminRole` 与新成员角色分开，前者只管理平台级操作。
2. `Tunnel`、`NodeGroup`、私有 `Node`、团队设置、流量与任务必须有 `workspace_id`（或通过有约束的父资源直接关联）。平台共享入口属于平台，不得被普通 workspace 修改；分配给 workspace 时显式授权。
3. 请求先完成身份认证，再从路由上下文解析允许访问的 workspace，所有 DB 读写以 workspace ID 过滤；查询工具默认要求该 ID，避免 `findUnique(id)` 后忘记授权。列表/详情/更新/删除/导出/下载均有同等边界。MySQL 不提供自动租户行级隔离，必须以应用层封装和越权测试兜底。
4. Redis key、队列任务、Socket room、配置指纹、流量归档和审计事件统一带 workspace ID 与实例 ID；Agent 凭证绑定 workspace + node + 版本，失效/转移时撤销并清理缓存。
5. 任何前端的按钮隐藏只是 UX；后端承担真实权限判定。平台管理员跨租户诊断需采用单独高权限审计流程，不能默认以用户身份读其他空间。
6. 额度按 workspace 统一判定，避免两个成员并发创建隧道绕过上限；把 `Plan` 等商业模型与基础资源配额解耦，不得以余额是否足够决定免费隧道能否使用。

### 4.2 权限、额度与支付彻底解耦（关键设计）

参考文档里的“套餐”实际同时承载**能力开关、节点组访问、资源配额和价格/续费**；后台 RBAC 则负责“谁可以读写什么管理资源”。TuneX 保留前面三类丰富的授权能力，价格成为可选的**获得授权方式**，而不是授权本身。

| 层次 | 回答的问题 | 核心字段/示例 | 是否需要支付 |
| --- | --- | --- | --- |
| 身份与租户 | 谁在访问哪个工作空间？ | 用户/Agent、workspace 归属、API Key 作用域 | 否 |
| 操作权限（RBAC） | 可以对哪类资源做什么？ | 角色 → 隧道/节点/成员/统计/后台的 `read/create/update/delete/manage`；`own/workspace/platform` 范围 | 否；团队角色不以授权版本或付费状态解锁 |
| 功能与准入（Entitlement） | 该空间允许使用什么？ | 协议类型、是否可自建入口/出口、允许的入口/出口节点组、可共享入口 | 否；由免费模板或人工授权即可获得 |
| 资源额度（Quota） | 最多能用多少？ | 最大隧道/节点/连接数，流量、带宽、IP 数，时间窗及生效/过期时间 | 否；可免费、人工、试用或付费发放 |
| 支付（Billing，可选） | 通过什么渠道获得/延长额度？ | 不可直接读写隧道权限；验证后的付款事件仅触发一条额度/能力授予或续期 | 只有实际启用支付才需要 |

**单一判定入口**：新增 `authorize(actor, action, resource, workspace)` 和 `getEffectivePolicy(workspace, actor?)`。API 先校验身份/租户/角色，再查功能准入与额度；NodeGroup 选择、隧道创建、Agent 配置下发都必须复用同一份策略快照。UI 的 `GET /api/me/capabilities?workspace_id=...` 返回允许的动作、可用节点组、限额、当前用量、拒绝原因和 `policy_revision`，只供展示，服务端仍重新判定。**禁止以是否付费/余额判断是否可管理或创建隧道。**

**策略合成**：系统安全上限 ∩ workspace 授权 ∩ 用户角色/资源归属 ∩ 节点组允许列表。多角色授予的操作权限在同一 workspace 内取并集，显式禁用/平台安全约束优先于授予；额度取平台硬上限与空间授予的较小值。字段语义明确：`0` = 禁止，`null` = 无额外额度限制（仅管理员明确赋予），空白名单 = 无访问，`allow_all` 必须显式为真；绝不因没有套餐或名单为空就默认放行。流量耗尽/授权到期时返回具体原因，并约定降级宽限期；安全撤权（删除成员、撤销 token）立即阻断新连接并清理缓存与配置。

**建议模型**：`WorkspaceRole` / `RolePermission`（或经过验证的权限 JSON，初版固定 owner/admin/member/viewer）；`CapabilityPolicy`（不含 `price`）；`WorkspacePolicyAssignment`（来源 `system_default/admin_grant/trial/purchase`，带生效、截止、撤销和版本）；`NodeGroupGrant`（租户与入口/出口白名单）；`UsageBucket`（带租户/计量周期）。保留 `Plan`、`UserPlan`、`PlanNodeGroup` 和订单表用于兼容，不直接当核心授权事实源。价格/SKU 若以后启用，映射到一项有明确期限的 PolicyAssignment，终止支付服务不影响已有免费授权。

**从当前代码迁移的准确落点**：

1. `backend/src/middlewares/auth.ts` 的 `adminRequired`/`adminPermissionGuard` 当前检查 `licenseService.isBusinessLicense()`，会使角色管理依赖“商业授权”。改为平台管理员权限或 workspace 角色校验；**不要通过把全部安装都伪装成 business 来绕过**。
2. `backend/src/socket/config-generator.ts` 的 `filterAvailableTunnels()` 依赖 `user.user_plan`，没有时会跳过隧道；把这部分改为 `getEffectivePolicy()`。当前白名单分支仅在 `groups.length > 0` 时才拒绝未授权组，空名单可能形成放行路径；按“默认拒绝”增加回归测试。
3. `backend/src/routes/plans.ts` 当前把购买、余额和 `UserPlan` 写入绑在一起。新用户创建 workspace 时**事务性发放免费策略**，无需购买订单；避免只给某个 UserPlan 导致团队成员权益不一致。
4. 原有 `AdminRole` 只描述平台后台资源；团队角色另建独立作用域，不把平台超管权限等同于租户 owner；权限变化需要使配置/缓存立即失效并推送 Agent。

**示例**：免费个人空间获“2 条 TCP 隧道、1 个私有节点、指定共享入口节点组”的策略；团队空间获“10 条隧道、5 个成员、2 个节点、TCP/UDP（UDP 真正通过测试后）”的策略，owner 能授权成员但 viewer 只能读取；这两份授权都可由系统或管理员直接发放，**与支付状态无关**。数值仅是设计示例，正式默认值应由容量测试决定。

### 4.3 控制与数据平面

- 控制面只保存配置与分发期望状态，不转发用户流量。私网 Agent 只需建立出站连接，入口端口受租户额度和安全策略约束。
- 独立设计 `tunex-v1` 协议，使用维护中的成熟 TLS/mTLS 与标准密码库；按环境生成/轮换凭证，明确认证、版本、重放保护与撤销。不得继续采用上游固定密钥、自签 license 或借用其 Agent；旧协议与新版本只在隔离测试环境兼容，生产不静默互通。
- 配置带 workspace ID、node ID、revision、过期时间和策略；Agent 验证后原子应用并回 ACK（已生效版本、失败端口、错误码）。同一 revision 幂等，重连后补偿推送，旧 revision 不覆盖新配置。
- TCP 先完成“公网入口 → 出站连接的私网节点 → 私网服务”全链路；UDP、WSS、QUIC、多跳与 HA 分别独立设计和发布，不能以当前枚举/模拟器推断支持。

### 4.4 商业能力的“弱化”而非删除

- 保留支付/套餐相关模型和历史订单，以 `payments.enabled = false` 和独立的运营功能开关控制 UI、API、回调、定时任务；**服务端拒绝**未开启功能的请求，不能只隐藏导航。
- 新用户直接获得免费 workspace 的基础权限和可用额度；不自动生成付费订单，不出现“先充值再使用”路径。平台可把策略分配给个人/团队并明确到期时间，免费团队也能使用完整的角色权限；升级只影响资源额度或可选附加服务，不剥夺安全的管理权限。
- 拆分授权/额度服务和可选 Billing Adapter：核心隧道域只询问“当前主体对此资源是否有操作权限、该 workspace 是否有能力与额度”，不直接查余额、交易或支付渠道。未来确需付费时，由经过签名/金额/幂等验证的支付事件**发放/续期策略**；停用支付仍保留免费和人工授权路径。
- 旧订单、支付、余额、佣金数据保持只读备份；不因本方案而删表或改写用户余额，敏感迁移须单独审查。

### 4.5 SaaS 运营基础

- 控制面生产环境最少独立 TLS、专用数据库账号、非公网 MySQL/Redis、限流与审计、按 workspace 的容量限制。当前 Compose 宿主机映射是开发默认，必须有独立的生产配置。
- 后台指标最少包括租户数/活跃隧道数、Agent 在线率、配置失败率、连接成功率、隔离拒绝次数和资源容量；指标不含明文 token/敏感目标地址。
- 提供数据保留、导出、工作空间删除与备份恢复流程；隐私/服务条款在对外开放注册前确定，第三方遥测默认关闭或明确披露。

## 5. 实施路线（每个切片打通 UI → API → DB/Agent → 测试）

**估算**：两名熟悉前后端/网络的开发者，公开 Beta 约 **10–16 周**；单人约 **16–24 周**。前提是合法来源审查可完成，且有 Linux 双节点和独立公网测试资源。阶段工期仅用于排期，不替代退出门槛。

### 阶段 0：审计、安全与真模式基线（1–2 周）

- 盘点源码/资产/依赖许可，隔离上游固定密钥和授权路径；列出必须独立重写的组件及发布阻断项。
- Linux 隔离环境拉起 MySQL/Redis/API/Web/Agent，关闭 mock；记录现有真实链路的成功/失败行为。核查开发默认管理员密码、JWT 秘密、明文端口和注册开关；生产配置缺失即启动失败。
- 默认关闭支付/充值/佣金/提现等服务端写入口、回调与定时任务，并隐藏引导按钮；仅保留历史只读数据，**不开删除迁移**。
- 建 Linux CI：Web 类型/构建、Backend 类型/构建/Prisma 迁移、Agent build/vet/test、空库启动；补基础网络 E2E 和秘密扫描。
- **验收**：隔离环境真模式基线可重复；生产无弱默认凭证；支付 HTTP 直连请求被拒绝；来源风险形成审查清单。

### 阶段 1：租户与账号安全底座（2–3 周，公开服务前必须完成）

- 实现个人与团队 workspace、固定 owner/admin/member/viewer 角色、邮箱验证、受邀注册和空间切换；平台后台和 workspace 管理权限分离。
- 迁移 `workspace_id` 与统一作用域查询；审查所有 API 和后台接口的列表、详情、更新、下载路径；Redis/Worker/Socket 的标识统一租户命名空间。新增免费的 CapabilityPolicy 与 workspace 策略分配、固定 owner/admin/member/viewer 权限矩阵，替换“没有套餐就没有权限”的核心路径。
- API Key、Agent token 与 workspace 绑定；创建/撤销、最小权限和过期策略；邀请链接单次使用/过期，注册/login/reset 端点限流。Cookie 会话启用 CSRF 保护、HTTPS 和安全属性；防止可信代理头被任意伪造。平台/团队 RBAC 不再依赖 `isBusinessLicense()`，且不能通过伪造商业授权绕行。
- **验收**：两 workspace、多账号、同 ID/同名资源、并发建隧道与 Agent 重连的隔离矩阵自动化通过；免费 owner 可分配角色与创建符合策略的隧道，viewer 不可写、空节点组白名单不放行、无订单用户正常获得免费策略；限流、Cookie、密钥撤销实测通过。

### 阶段 2：真实 TCP 隧道纵切片（2–4 周）

- 替换/隔离参考项目的共享密钥与授权语义，实现独立的控制面配置契约和认证加密反向数据链路；私网节点不能依赖公网入站端口。
- 从真实 UI 完成“部署 Agent → 绑定工作空间/入口 → 建 TCP 隧道 → 外部访问 → 停用/删除”；API 创建校验和 Agent 下发共用同一授权策略，后端原子分配端口与额度，多 Agent 同组并发时不发生冲突。
- 上报配置 ACK、监听错误、Agent 连接失败；前端展示实时状态与明确原因，演示数据仅存在开发 mock 模式。
- **验收**：隔离网络中两套租户、每套公网入口与无入站端口私网 Agent 的端到端 TCP 连通；跨租户 token、端口/配置串用与恶意目标地址被阻断；断线/重启后正确恢复。

### 阶段 3：可靠性、额度和观测（1–2 周）

- 只注册已实现的 worker 任务：TTL 离线检测、重连状态恢复、必要的配置重推、流量缓冲落盘与清理；明确幂等键和失败重试上限。
- 落实工作空间免费策略：节点组/协议准入、隧道数、节点数、IP/并发/带宽/流量限制及到期策略，限额/禁止原因可解释；超限不引导充值。流量数据真实采集、按 workspace 聚合并明确保留期。
- 灾备：生产 DB/Redis/配置备份，版本化镜像、滚动更新、失败回滚；容量测试基线和监控告警。
- **验收**：Agent 异常退出后 <= 60 秒显示离线，恢复后 <= 15 秒配置有效；两 workspace 并发额度不互相影响；备份恢复和 100 并发 TCP 基础测试通过（资源指标按首轮压测锁定）。

### 阶段 4：团队体验与 SaaS Beta（2–3 周）

- 在四个固定角色之上实现自定义资源读写角色、邀请与撤销、隧道级共享、策略模板分配及操作审计；清理商业首页文案，默认展示“创建工作空间/隧道”而不是充值套餐。
- 建用户旅程 E2E：个人注册 → 建隧道、团队邀请 → 授权成员使用/只读、跨团队被拒、管理员诊断带审计；完善手机/键盘体验。
- 完成生产注册策略、滥用防护、数据保留和删除请求、部署/运维/隐私/安全文档，私测观察后逐步开放注册。
- **验收**：至少一个个人 workspace + 两个相互独立的团队 workspace 同时运行；越权矩阵全绿、无可用收费写入口、告警/备份回滚演练通过，可标记 `0.1 SaaS Beta`。

### 阶段 5：后续扩展（按实测与需求排期）

- 可选 UDP、多入口、多跳及协议扩展分别立项，每个协议独立威胁建模、兼容测试与性能基线；未通过不进入产品能力列表。
- 只有在明确需要收费时，才开发受隔离的可选计费适配层，启用支付需经过产品决策、合规审查、回调金额/签名/幂等/退款等完整验证。默认免费核心流程仍可用。
- 对照真实运维数据决定是否增加控制面多副本、地区容灾或自托管版本；避免为“像企业平台”预先扩大复杂度。

**关键路径**：来源/生产秘密审查 → 关闭不成熟支付入口 → workspace 隔离与 token 绑定 → 私网无入站的独立 TCP 链路 → 在线状态/配额 → 多租户真模式 E2E → SaaS Beta。UI 与文档可并行，但隔离和协议验收不可跳过。

## 6. 可直接建 Issue 的工作包

| 编号 | 级别 | 状态 | 交付成果 | 代码落点 / 依赖 | 验收凭证 |
| --- | --- | --- | --- | --- | --- |
| SEC-01 | P0 | ✅ 完成 | 来源/许可证审查；移除上游固定密钥/授权依赖 | `backend/src/crypto/`、`agent/internal/` | main 已合入；密钥缺失启动 fail-fast |
| SEC-02 | P0 | 🟡 部分 | 生产配置检查、用户认证/限流/CSRF、邀请与密钥轮换 | `backend/src/env.ts`、`backend/src/routes/auth.ts`、`middlewares/rate-limit.ts` | fail-fast ✅、限流 ✅；CSRF/密钥轮换待做 |
| TEN-01 | P0 | 🟡 后端完成 | Workspace/Membership/Invite 迁移和平台管理员分离 | Prisma、`backend/src/routes/workspaces.ts`、`services/workspace.ts` | CI 迁移升级验证 ✅；**前端与邮箱验证待做** |
| TEN-02 | P0 | 🟡 部分 | API/Redis/Queue/Socket/Agent 全链路租户作用域 | `routes/`、`socket/`、`worker.ts` | 隧道/节点组查询已带 workspace；Redis key 作用域未统一 |
| NET-01 | P0 | 🟡 部分 | 自有 TCP 反向数据通道与节点身份验证 | `agent/internal/`、后端配置分发 | 单节点主链路实测通；双租户真实网络 E2E 待做 |
| NET-02 | P0 | ✅ 完成 | 幂等配置 ACK、冲突处理、监听失败反馈 | `socket/listen-events.ts`、`port-allocator.ts`、`config-pusher.ts` | 离线测试 30+ 用例全过 |
| AUTHZ-01 | P0 | ✅ 完成 | RBAC 去商业授权闸门，建立 workspace 角色/资源/动作矩阵 | `middlewares/auth.ts`、`services/node-group-policy.ts` | 未付费 owner 按角色管理资源，CI 通过 |
| AUTHZ-02 | P0 | 🟡 大部分 | CapabilityPolicy + Assignment + NodeGroupGrant，替换 UserPlan 过滤依赖 | `services/capability-policy.ts`、`policy-service.ts`、`socket/config-generator.ts` | 策略体系与 NodeGroupGrant ✅；**config-generator 的 user_plan 依赖替换待收尾** |
| SOFT-01 | P0 | 🟡 大部分 | 策略额度原子判定、到期/流量耗尽处理 | `services/policy-service.ts`（FOR UPDATE 并发锁已实现） | 服务层就绪；**路由接线与并发用例待收尾** |
| PAY-01 | P0 | ✅ 完成 | 关闭支付写入口和回调但保留历史数据 | `middlewares/payments-gate.ts`、前端开关 | 直接调用与伪造回调均 403，CI 通过 |
| OPS-01 | P0 | 🟡 部分 | 离线状态、流量聚合与任务幂等 | `worker.ts`、`socket/offline-detector.ts` | 离线检测闭环 ✅；**流量入库/聚合仍空壳** |
| QA-01 | P0 | ✅ 完成 | Linux CI、空库升级迁移、双租户网络+浏览器 E2E | `.github/workflows/ci.yml` | 真实 MySQL CI + 秘密扫描 + GHCR 镜像 ✅；**双租户 E2E 待做** |
| OPS-02 | P1 | ⚪ 未开始 | SaaS 部署/备份/告警/容量基线与回滚 | Compose 生产配置、Caddy、文档 | GHCR 预构建镜像已就位；备份/告警待做 |
| TEAM-01 | P1 | ⚪ 未开始 | 自定义团队角色与细粒度审计（基础权限先在 P0 落地） | Prisma、后台权限、Web 设置 | 固定四角色已落地 |
| BILL-01 | P2 | ⚪ 未开始 | 独立可选计费适配层（有需求后再排） | 不污染核心隧道域 | 支付关闭状态已满足前置 |
| OPS-03 | P0 | ⚪ 新增 | 流量计量链路：agent 上报 → Redis 缓冲 → MySQL 入库 → 按 workspace 聚合展示 | `worker.ts` cron_save_traffic、`services/`、Web 仪表盘 | 采集入库不重复，聚合口径与策略流量一致 |
| TEN-03 | P0 | ⚪ 新增 | 邮箱验证与密码重置 | `routes/auth.ts`、邮件服务 | 注册验证邮件、重置链接单次使用/过期 |

每个工作包需写清：状态迁移、API 契约、跨租户负面用例、可观测性、开发配置、回滚方法。P0 未完成不可公开对外提供多租户服务。

## 7. 验证、发布与运行门槛

1. **静态和单元**：Web TypeScript/build；Backend Bun 类型/build 与 Prisma 迁移；Linux Go build/vet/test；鉴权与角色矩阵、能力合成、策略到期/撤销、额度并发、配置版本、worker 幂等均有回归测试。现有 Go 代码使用 Linux syscall，不以 Windows 原生编译作为发布门槛。
2. **多租户安全矩阵**：至少 A/B 两团队 + 个人空间，普通用户、已撤销成员、只读成员、平台管理员分别测列表/详情/更新/删除/导出/API Key/Agent 配置/Socket 推送/Redis/Queue；跨租户访问不得返回内容或可用 token。
3. **真实网络**：不靠 mock/localhost，自外部客户端访问两套互相隔离的公网入口与私网 Agent；测正常、错误身份、端口冲突、断网/重连、重复配置、目标拒绝、并发和升级。
4. **浏览器 E2E**：注册/邮箱验证/空间创建/邀请/建立隧道/查看失败状态/切换空间/越权、移动端/键盘；强商业入口默认不可见且后端不可用。
5. **发布前安全**：第三方来源审查、依赖与秘密扫描、TLS 证书、限流/滥用防护、生产端口隔离、日志脱敏与备份恢复；发现 P0 缺口禁止上线。
6. **灰度与回退**：先内部/邀请制 Beta；服务端按 workspace 控制新功能开关。发布保留上一版本镜像与数据库备份，问题时先回退服务再进行有状态迁移；不得自动删用户数据。

## 8. 迁移现有代码与数据的方法

- **先备份，后加列**：新增 Workspace/Membership，老用户各创建默认个人空间；原有隧道、私有节点、API Key 按可验证所有权回填，归属不明的记录进入人工待处理队列，不允许静默绑定到第一位管理员。
- **共享入口明确所有权**：由平台管理的入口节点与租户自己的私网节点分开，配额/端口分配记录 workspace；防止共享节点配置向错误租户推送。
- **先双读审计，后强制作用域**：在隔离环境比较迁移前后数据；新的写路径只接受 workspace ID；业务查询完成作用域覆盖后停用旧无作用域路径。
- **商业与权限数据双轨迁移**：旧 `Plan`/`UserPlan`/`PlanNodeGroup` 的有效能力快照映射到新策略分配（来源/生效/截止/节点组白名单可追溯）；旧订单/余额/佣金只读保留且不直接参与新的授权判定。旧记录有歧义时人工确认，不把空白名单转换成全量授权。任何财务类数据清理都需要独立审批与回退演练。
- **协议过渡**：旧 `relayx` 类型、Cookie、环境变量、镜像/容器名不能机械替换；定义迁移映射和最低兼容版本，旧 Agent 上线前明确提示升级。旧固定密钥禁止进入新 SaaS 生产环境。
- **数据库版本化**：采用向后兼容的“新增→回填→切换→清理”迁移；每一步有数据库快照、数据量检查、回滚或前向修复方案。

## 9. 风险与待确认项

| 风险/假设 | 严重性 | 判定与处理 |
| --- | --- | --- |
| 当前密钥/协议/授权来源可能不适合独立 SaaS 发布 | 阻断 | 独立来源审查和替换；未闭环只在隔离环境测试 |
| 现有数据无 workspace 归属 | 阻断 | 迁移时逐行确认，租户越权矩阵在公开前通过 |
| 支付回调与收费 API 虽不显示仍可被调用 | 阻断 | 后端强制开关 + 路由/回调负面测试 + 不调度收费任务 |
| Agent 功能名称超出实际验证范围 | 高 | 只展示已通过端到端的 TCP，其余协议按独立里程碑发布 |
| 定时任务与报表存在占位实现 | 高 | 只注册已实现工作，指标注明真实性和更新时间 |
| 公开 SaaS 招致端口滥用与数据风险 | 高 | 账号/工作空间配额、目标地址策略、速率限制、审计和隐私/滥用处理机制 |
| “非商业版”与免费 SaaS 的经济模型尚未明确 | 中 | 先提供默认免费额度和管理员可调整的资源限制；是否开启自愿付费另行决策，不阻塞核心产品 |

**需要产品负责人确认但不阻塞技术底座的事项**：① 平台是否允许公开注册，还是先邀请制；② 免费 workspace 的初始额度与目标地址策略；③ 后续是否开放可选支付；④ 是否对外开源及许可。默认采用“邀请制 Beta、免费基础额度、支付关闭、未确认许可前不公开源码”。

## 10. 完成定义

TuneX 能称为“个人与团队 SaaS Beta”的条件不是把原项目换一个名字，而是：**同一托管控制面存在多个隔离的个人/团队工作空间，私网节点无需入站端口且真实 TCP 连通，越权/串租户测试全绿，支付可选且默认关闭，旧固定密钥和上游授权机制已替换，离线/回滚/备份可验证，Linux CI 和安全审查通过**。其他协议、商业能力和自托管发布须各自另立验收记录。

## 11. 实施记录（2026-09-24，进行中）

- TEN-01/02 持续开发：新增个人/团队 Workspace、成员、邀请、工作空间 API Key 占位与审计模型；注册和旧库资源回填已接线，团队节点组与隧道按 workspace 作用域读写。用户级 API Key 暂不能管理团队，邀请凭证仅哈希存储。**仍未完成**成员变更触发的 Agent 凭证撤销、按空间计量的能力/额度、所有边缘 API 与 Redis/Socket 的租户审查；空库迁移、既有双用户资产回填及真实 MySQL/Redis 多租户测试已在 GitHub Actions 35960083478 通过；仍需扩展至全链路租户隔离和生产回滚验收。

- 旧账户订阅密钥限定为个人空间（团队订阅须独立工作空间凭证），仪表盘流量、隧道和节点统计按已授权空间隔离；已补真实 MySQL 越权/撤员回归测试，待新一轮 CI 运行确认。
- 已移除后台 RBAC、API Key、工单对商业 License 的闸门；**只解决平台级权限的一部分**，workspace 角色矩阵尚未开始，AUTHZ-01 未完成。
- 新增按用户/节点组/入口或出口的 NodeGroupGrant 迁移和管理端授权接口；隧道创建、修改和配置下发按所有权或显式授权检查；无套餐用户的自有隧道不再被配置生成跳过。当前仍保留 UserPlan 的历史额度限制，CapabilityPolicy/WorkspacePolicyAssignment/工作空间隔离尚未完成，AUTHZ-02 未完成。旧套餐中共享节点组关系**不会自动升级**为显式授权，管理员必须审核后逐项发放。
- 支付默认关闭；服务端拦截支付写入口和网关回调，历史记录读取保留；前端默认隐藏购买/充值导航。PAY-01 仍需真实 HTTP、伪造回调及 worker 验收（worker 现为占位骨架）。
- 已增加 `.github/workflows/ci.yml`，提交 `d39465a` 的 GitHub Actions 运行 `35958068306` 三个作业（backend/web/agent）全部成功：MySQL 8.4 空库迁移、Prisma 生成与类型检查、支付/授权测试、Web 生产构建、Go vet/test/build。另本机已验证 Prisma Schema 与迁移差异、Backend tsc、Web 构建及策略/HTTP 冒烟。未验证：**既有生产库升级与回滚**、Linux 多节点、双租户 E2E、支付开启场景及备份恢复；不得将以上标记为 SaaS Beta 验收通过。本机无 Docker CLI。
